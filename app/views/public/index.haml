%h2 Beers
- @beers.each_slice(3) do |slice|
  .row
  - slice.each do |beer|
    .col-md-4
      .thumbnail  
        = beer.photo.present? ? (image_tag beer.photo.url(:normal), class: 'img-thumbnail') : '---'
        .caption
          %h3= beer.name
          %h4
            = link_to beer.style.name, beers_main_path(style_id: beer.style.id)
            from 
            = link_to beer.brewery.name, beers_main_path(brewery_id: beer.brewery.id)
          %p
            -rating_stars(beer.rating).each do |image|
              = image
          %p= short_desc(beer.description)
.col-md-12.text-center.apple_pagination= will_paginate @beers